import { SelectlineUniversalImporterGuard } from './selectline-universal-importer.guard';

describe('SelectlineUniversalImporterGuard', () => {
  it('should be defined', () => {
    expect(new SelectlineUniversalImporterGuard()).toBeDefined();
  });
});
